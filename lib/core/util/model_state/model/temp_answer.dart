class TempAnswer {
  final String? id;
  final String? answer;
  final bool? isAnswer;
  final bool? isActive;

  const TempAnswer(
      {this.id, this.answer, this.isAnswer = false, this.isActive = false});
}
